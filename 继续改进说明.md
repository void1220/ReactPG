# Codex 项目改进说明：OpenExp 实验步骤预测中的骨架条件图补全与数值槽位落地

## 1. 当前项目背景与实验现象

本项目面向 OpenExp 27 万级数据集，核心任务是根据输入实验条件、材料信息和反应上下文预测实验方案步骤。现有方法采用“**AR 生成主要操作骨架 + 离散扩散补全其他节点图 + 最后 AR 补全数值**”的框架。当前实验结果显示，该框架比“完整过程图全部使用扩散生成”更好，测试集指标提升约 5% 以上，说明**固定操作骨架再做图补全的方向是正确的**。但是整体泛化效果仍然不足，目前测试集约为 **LEV75≈10%，LEV50≈38%**；同时在训练集上，无论是否使用 AR 生成骨架，都能达到 90% 以上命中率。这说明模型容量和训练集拟合能力不是瓶颈，主要问题是**测试泛化不足、图补全任务与推理场景不完全一致、数值生成与材料/操作/条件关系脱节**。

因此，下一阶段不应优先盲目将离散扩散替换为连续时间离散图扩散，而应先改进表示、训练任务、数值处理与评估体系。连续时间离散图扩散可以作为后续升级，但必须建立在更稳定的槽位图表示和数值证据绑定机制之上。

---

## 2. 总体改进目标

本次代码改进的核心目标是：**保持 AR 操作骨架生成作为主干，降低图扩散补全的开放生成难度，将材料、条件、数值从自由生成转为槽位化、指针化和证据约束的结构化补全任务**。最终希望在不明显损失 LEV90/LEV75/LEV50 的前提下，提高结构完整度、数值稳定性、可解析性和安全一致性。

本阶段不要直接重写整个系统，也不要优先实现复杂的离散—连续联合扩散。请优先完成以下几类代码能力：

1. 加入诊断实验，确认当前瓶颈来源；
2. 将图补全目标从开放节点生成改为 slot-based process graph completion；
3. 将材料实体生成改为 pointer/copy 到输入材料或候选材料；
4. 将数值生成改为 evidence-aware numeric slot grounding；
5. 加入 number-normalized 评估、numeric slot binding 评估和 graph-to-action 解码上界评估；
6. 加入 gold skeleton / predicted skeleton 混合训练，缓解训练—推理分布不一致。

---

## 3. 需要优先完成的诊断实验

请优先实现以下诊断脚本或配置开关。诊断结果比直接调大模型更重要。

### 3.1 Ground-truth graph → action/text 解码上界

目的：判断当前图表示和解码器是否本身限制了 LEV 上界。

实现方式：

- 输入测试集 ground-truth process graph；
- 不经过任何生成模型，直接用现有 deterministic graph-to-action/text decoder 生成输出；
- 计算 BLEU、ROUGE、LEV90、LEV75、LEV50；
- 同时计算 number-normalized LEV，即将所有数值和单位片段替换为统一标记后再计算 LEV。

预期解释：

- 如果 ground-truth graph 解码后 LEV75 仍然很低，说明主要问题是图表示或解码器丢失了文本相似度所需信息；
- 如果 ground-truth graph 解码上界很高，但模型预测图输出低，说明主要问题在图补全模块。

建议新增脚本：

```bash
python scripts/diagnose_gt_graph_decode.py \
  --data_dir <DATA_DIR> \
  --split test \
  --output_dir outputs/diagnostics/gt_graph_decode
```

### 3.2 Gold skeleton test evaluation

目的：区分 AR 骨架错误和图补全错误。

实现方式：

- 测试时使用 ground-truth skeleton；
- 只让图补全模块预测材料、条件、依赖边、数值槽位等非骨架部分；
- 与使用 predicted skeleton 的结果对比。

建议新增配置：

```yaml
eval:
  skeleton_source: gold   # gold | predicted
```

### 3.3 Predicted skeleton quality evaluation

目的：单独评估 AR 骨架的泛化能力。

需要输出：

- operation type accuracy；
- operation sequence edit distance；
- operation coverage；
- skeleton LEV；
- skeleton length error；
- top-k skeleton oracle score。

建议新增脚本：

```bash
python scripts/eval_skeleton.py \
  --pred outputs/skeleton/pred_test.jsonl \
  --gold data/processed/test.jsonl
```

### 3.4 Graph diffusion vs deterministic/masked denoising classifier

目的：判断扩散是否真的优于普通监督补全。

实现方式：新增一个非扩散图补全 baseline：

```text
G_skeleton + context -> slot labels + edge labels + numeric slot types
```

如果该 baseline 测试效果优于多步扩散，说明当前扩散噪声过程没有帮助泛化，应先转为 masked denoising completion。

---

## 4. 图表示改进：从开放节点图改为槽位化过程图

当前模型如果直接生成具体材料节点、属性节点和数值文本，容易在训练集记忆、测试集泛化失败。请将过程图表示改成 slot-based ordered process graph。操作骨架仍由 AR 生成，图模块只补全固定槽位及其关系。

### 4.1 操作骨架保持不变

AR 模型生成：

```text
ADD -> STIR -> HEAT -> COOL -> FILTER -> WASH -> DRY
```

对应固定骨架图：

```text
V_op = {op_1, ..., op_T}
E_next = {(op_1, op_2), ..., (op_{T-1}, op_T)}
```

这些操作节点和 next 边在图补全阶段固定，不参与扩散或随机破坏。

### 4.2 每个操作节点展开固定槽位

每个操作节点建议展开如下槽位，所有槽位都允许 `NULL`：

| 槽位类型 | 含义 | 是否由扩散/补全模型预测 |
|---|---|---|
| input_material_slot | 输入材料、试剂、溶剂等 | 是 |
| output_state_slot | 中间状态、产物状态、溶液/固体等 | 是 |
| condition_slot | 温度、时间、气氛、搅拌、加热等条件类型 | 是 |
| equipment_slot | 容器、过滤、干燥、浓缩等设备或过程对象 | 可选 |
| numeric_requirement_slot | 是否需要数值、数值类型、单位类型 | 是 |
| safety_constraint_slot | 抽象安全/约束标签 | 可选 |

注意：图模块不应直接生成开放自然语言节点，而应预测槽位类别、槽位是否存在、槽位与操作/材料的关系。

建议新增数据结构：

```python
@dataclass
class ProcessSlot:
    op_index: int
    slot_type: str
    slot_index: int
    label: str | None
    is_null: bool
    source: str | None = None
```

### 4.3 候选边集合限制

不要在所有节点对之间预测边。只在合法候选边集合中预测：

```text
operation -> material_slot
operation -> condition_slot
operation -> numeric_slot
operation -> output_state_slot
operation_i -> operation_j, where j > i
numeric_slot -> numeric_candidate
material_slot -> input_material_candidate
```

非法边不要进入模型候选集合，或者在 decoder 中用 grammar mask 强制置零。

---

## 5. 材料实体改进：从生成材料名改为 pointer/copy

材料实体不应由扩散模型或 AR 模型从词表中自由生成。请改为 pointer/copy 机制。

### 5.1 输入材料候选池

从输入条件、反应物、试剂、溶剂、产物、检索样例中构造候选材料池：

```python
material_candidates = [
    {"id": "MAT_0", "text": "...", "role_hint": "reactant"},
    {"id": "MAT_1", "text": "...", "role_hint": "solvent"},
    ...
]
```

图补全模型预测：

```text
input_material_slot(op_i, k) -> MAT_j
```

而不是直接输出材料字符串。

### 5.2 材料槽位训练目标

增加 pointer classification loss：

```text
L_material_pointer = CE(pred_candidate_id, gold_candidate_id)
```

如果某个材料不在候选池中，使用 `UNK_MATERIAL` 或 `EXTERNAL_MATERIAL`，不要强行生成具体字符串。

---

## 6. 数值模块改进：证据感知的数值槽位落地

当前“最后单独用 AR 补全数值”的方式应替换为 **Evidence-aware Numeric Slot Grounding**。核心思想是：**放弃自由生成精确数值，但不放弃数值建模**。模型需要预测哪里需要数值、数值类型是什么、单位类型是什么、应绑定到哪个候选证据；无可靠证据时允许输出缺失或低置信候选。

### 6.1 数值槽位需求预测

对每个操作节点和材料/条件关系，预测是否需要数值槽位：

| 数值槽位类型 | 示例 |
|---|---|
| amount | 质量、体积、摩尔量、当量 |
| temperature | 温度、低温、室温、回流温度等 |
| duration | 时间、搅拌时长、加热时长等 |
| concentration | 浓度、比例 |
| yield | 产率、产物质量 |
| repetition | 次数 |

模型输出：

```python
numeric_slot = {
    "op_index": i,
    "slot_type": "temperature",
    "required": True,
    "unit_type": "temperature",
    "value_bucket": "0_25C",  # 可选
}
```

### 6.2 数值候选池构建

数值候选来自证据，不由模型自由生成。候选来源按优先级：

| 来源 | 优先级 | 说明 |
|---|---:|---|
| 输入实验条件中显式数值 | 最高 | 直接候选 |
| 材料信息附带数值 | 高 | 与材料槽位绑定 |
| 文献检索/上一课题大模型给出的条件约束 | 高 | 作为外部 evidence |
| 相似反应/相似步骤中的数值 | 中 | 弱候选 |
| 训练集中操作类型常见范围 | 低 | 只能用于 bucket 或低置信默认值 |

建议数据结构：

```python
@dataclass
class NumericCandidate:
    candidate_id: str
    raw_text: str
    value: float | None
    unit: str | None
    normalized_value: float | None
    normalized_unit: str | None
    numeric_type: str
    source: str   # input | retrieval | similar_case | prior | model_inferred
    confidence: float
    linked_material_id: str | None = None
    linked_op_hint: int | None = None
```

### 6.3 数值槽位绑定

模型预测：

```text
numeric_slot(op_i, type=temperature) -> NUM_CAND_k
```

而不是直接输出 `0°C` 或 `36 hours`。

训练损失：

```text
L_numeric = L_required + L_numeric_type + L_unit_type + L_candidate_pointer + L_bucket
```

其中 `L_candidate_pointer` 是最关键项。

### 6.4 无证据数值处理

如果候选池没有可靠候选，请不要强制生成精确数值。允许输出：

```text
<NUMERIC_SLOT_MISSING>
```

或者：

```text
numeric_type=duration, value_bucket=medium, confidence=low
```

最终文本生成时可以选择跳过低置信数值，或标记为待补全。对于 OpenExp 离线评估，可以仍然输出最可能候选，但必须记录 `source` 和 `confidence`，用于后续分析。

---

## 7. 训练任务改进

### 7.1 混合 gold skeleton 与 predicted skeleton

训练图补全时不能只使用 gold skeleton。请实现 predicted skeleton cache，并在训练时混合输入。

建议比例：

| 训练阶段 | gold skeleton | predicted skeleton | corrupted skeleton |
|---|---:|---:|---:|
| warmup | 0.8 | 0.2 | 0.0 |
| middle | 0.5 | 0.3 | 0.2 |
| final | 0.3 | 0.4 | 0.3 |

需要新增配置：

```yaml
training:
  skeleton_mix:
    gold_ratio: 0.5
    predicted_ratio: 0.3
    corrupted_ratio: 0.2
  predicted_skeleton_cache: outputs/skeleton/train_topk.jsonl
```

### 7.2 任务噪声替代纯随机噪声

不要只使用均匀类别替换噪声。请加入贴近真实错误的任务噪声：

| 噪声类型 | 模拟错误 |
|---|---|
| delete_material_slot | 漏掉材料 |
| delete_condition_slot | 漏掉条件 |
| delete_numeric_slot | 漏掉数值 |
| wrong_material_pointer | 材料绑定错误 |
| wrong_numeric_candidate | 数值绑定错误 |
| drop_role_edge | 角色边缺失 |
| add_spurious_edge | 多余关系 |
| span_mask_steps | 连续步骤局部缺失 |
| corrupted_skeleton | AR 骨架错误 |

建议先实现 masked denoising，再考虑多步离散扩散。

### 7.3 增加 action-level loss

当前图补全模型如果只优化图重建，可能对 LEV 没有帮助。请加入 completed graph 解码后的 action sequence loss 或 teacher-forced action reconstruction loss。

总损失建议：

```text
L = L_slot + λ1 L_edge + λ2 L_material_pointer + λ3 L_numeric + λ4 L_action
```

其中：

- `L_slot`: 槽位类型与存在性；
- `L_edge`: role/dependency edge；
- `L_material_pointer`: 材料候选指针；
- `L_numeric`: 数值槽位与候选绑定；
- `L_action`: 图解码后的 action sequence 对齐。

---

## 8. 解码与后处理

### 8.1 Deterministic graph-to-action decoder

图到 action sequence 应尽量确定性，不要让 LLM 自由改写。建议流程：

```text
completed process graph
-> canonical action sequence
-> optional lightweight surface realization
```

先保证 canonical action sequence 稳定，再考虑自然语言表面化。

### 8.2 Verifier 与 reranker

每个样本生成多个候选，例如 top-5 或 top-10，然后用综合分数重排：

```text
score = α * AR_logprob
      + β * graph_validity
      + γ * numeric_grounding_score
      + δ * parser_pass_score
      - η * unsupported_numeric_penalty
```

建议 verifier 检查：

| 检查项 | 说明 |
|---|---|
| graph grammar pass | 图结构是否合法 |
| skeleton continuity | 操作顺序是否连续 |
| slot completeness | 必需槽位是否完整 |
| material pointer validity | 材料指针是否来自候选池 |
| numeric pointer validity | 数值是否有证据来源 |
| unit normalization pass | 单位是否可规范化 |
| unsupported numeric rate | 是否凭空生成数值 |
| parse/compile pass | 是否能解析/编译为标准 action sequence |

---

## 9. 新增评估指标

除了原有 BLEU、ROUGE、LEV90、LEV75、LEV50，请新增以下指标：

| 指标 | 说明 |
|---|---|
| number-normalized LEV90/75/50 | 将数值归一为 `<NUM>` 后计算 LEV |
| operation F1 | 操作类型覆盖 |
| operation order accuracy | 操作顺序准确性 |
| slot completeness | 槽位完整度 |
| material pointer accuracy | 材料候选指针准确率 |
| material role F1 | 材料角色边 F1 |
| numeric slot required F1 | 是否需要数值槽位 |
| numeric type accuracy | 数值类型准确率 |
| unit accuracy | 单位准确率 |
| numeric candidate pointer accuracy | 数值候选绑定准确率 |
| unsupported numeric rate | 无证据数值比例 |
| graph grammar pass rate | 图语法通过率 |
| parse/compile pass rate | 可解析/可编译率 |

特别注意：如果 number-normalized LEV 明显高于原始 LEV，说明模型主要输在数值表达；如果二者都低，说明结构和操作顺序仍有问题。

---

## 10. 推荐代码实现优先级

请按以下顺序改进，不要一开始重构所有模块。

### P0：诊断与评估

1. 实现 ground-truth graph 解码上界；
2. 实现 gold skeleton / predicted skeleton 测试切换；
3. 实现 number-normalized LEV；
4. 实现 skeleton quality evaluation。

### P1：表示改造

1. 实现 slot-based process graph 数据结构；
2. 实现材料候选池和 material pointer；
3. 实现数值候选池和 numeric candidate parser；
4. 实现 graph-to-canonical-action decoder 的稳定输出。

### P2：模型改造

1. 实现 masked denoising graph completion baseline；
2. 加入材料 pointer head；
3. 加入 numeric slot requirement/type/unit/candidate pointer heads；
4. 加入 action-level auxiliary loss。

### P3：训练一致性

1. 生成 predicted skeleton cache；
2. 混合 gold/predicted/corrupted skeleton 训练；
3. 加入任务噪声。

### P4：reranking 与 verifier

1. 生成多候选；
2. 加入 graph/numeric/verifier score；
3. 实现最终候选重排。

### P5：后续扩展

在 P0–P4 稳定后，再考虑：

1. typed-rate continuous-time discrete diffusion；
2. absorbing MASK CTMC；
3. grammar-masked reverse rate；
4. 数值候选附近的连续校准模型。

---

## 11. 重要设计原则

请严格遵守以下原则：

1. **不要让模型自由生成精确数值**。数值应来自输入、检索、相似样本或候选池，模型负责绑定和规范化。
2. **不要让图扩散生成开放材料名**。材料必须 pointer 到候选材料。
3. **不要让图补全模型破坏 AR 骨架**。操作节点和 next 边应固定或只在诊断实验中人为扰动。
4. **不要只优化图重建损失**。必须引入 action-level 或 decoder-level 对齐，否则图正确不一定提升 LEV。
5. **不要在当前泛化不足时优先引入复杂连续时间扩散**。先验证表示和训练任务是否有效。
6. **保留旧模型作为 baseline**。所有新模块必须可通过配置关闭，便于消融。

---

## 12. 建议配置结构

建议新增配置项如下：

```yaml
model:
  use_slot_graph: true
  use_material_pointer: true
  use_numeric_grounding: true
  graph_completion_type: masked_denoising  # masked_denoising | discrete_diffusion | ct_discrete_diffusion
  freeze_skeleton: true

numeric:
  enabled: true
  free_generation: false
  candidate_sources:
    - input
    - retrieval
    - similar_case
    - prior
  allow_missing_when_no_evidence: true
  normalize_units: true
  use_value_bucket: true

training:
  use_action_aux_loss: true
  use_task_noise: true
  skeleton_mix:
    gold_ratio: 0.5
    predicted_ratio: 0.3
    corrupted_ratio: 0.2

eval:
  compute_number_normalized_lev: true
  compute_slot_metrics: true
  compute_numeric_metrics: true
  skeleton_source: predicted  # gold | predicted
```

---

## 13. 本轮改进的验收标准

本轮代码改进不要求立刻全面超过 ReactXT/ReactGPT，但应至少满足以下验收条件：

1. 可以输出 ground-truth graph 解码上界；
2. 可以分别报告 gold skeleton 和 predicted skeleton 下的图补全效果；
3. 可以报告 number-normalized LEV，并与原始 LEV 对比；
4. 可以报告材料 pointer、数值 slot、单位、候选绑定等结构化指标；
5. slot-based masked denoising baseline 至少不低于当前离散扩散补全结果；
6. unsupported numeric rate 明显低于原有 AR 数值补全方式；
7. graph grammar pass rate 和 parse/compile pass rate 高于当前版本；
8. 所有新增模块均可通过配置开关关闭，以保留旧方案作为 baseline。

---

## 14. 最终期望方向

本项目最终应形成如下稳定框架：

```text
reaction/material/condition input
-> AR operation skeleton generation
-> slot-based process graph completion
-> material pointer grounding
-> evidence-aware numeric slot grounding
-> deterministic graph-to-action decoding
-> verifier/reranker
-> final experimental procedure
```

该框架的核心优势不是让扩散模型从零生成所有内容，而是将实验步骤预测拆解为更加可泛化的结构化子任务：**操作顺序由 AR 决定，材料与条件关系由图补全决定，数值由证据绑定决定，最终由确定性解码和 verifier 保证完整性、稳定性与安全一致性**。
